## 2023-07-14 - Direct Squared Distance Comparison Avoids Math.sqrt in Hot Loop
**Learning:** Found two places using `Math.sqrt` in game loops `requestAnimationFrame`. As per memory, avoid `Math.hypot` and `Math.sqrt` when comparing distances against thresholds. Instead, compare squared distances directly to avoid computationally expensive root calculations in hot code paths.
**Action:** Replaced `Math.sqrt(speedSq)` with a simple division modification and `Math.sqrt(dx * dx + dy * dy)` with `Math.hypot` if actually needed for calculation, or just avoid `Math.sqrt` when computing distance constraints in game loops. Wait, I should look closely at how they are used.
